# GIS 质检工具 QA 测试报告

测试时间：2026-05-24
测试对象：D:\jiedan\136\gis-qc-workbench
测试人员：Hermes 自动化测试

## 1. 测试数据

已生成合成测试数据目录：

- `qa_synthetic_data/01_正常基础包`
- `qa_synthetic_data/02_缺失_命名_配套文件问题包`
- `qa_synthetic_data/03_GPKG空间几何问题包`

覆盖场景：

- 中文目录名、中文文件名
- 正常 GeoPackage 数据包
- 缺少必选目录、缺少 metadata.xml
- 空目录
- 非规范文件名
- Shapefile 缺少 sidecar 文件
- GPKG 线碎线、Z 值、尖锐角、线重叠/相交等几何问题
- CSV/HTML/XLS/DOC 报告导出

## 2. 自动化构建与单元测试

### Release 构建

结果：通过

生成：

- `build-windows-release/Release/GISQCWorkbench.exe`
- `build-windows-release/Release/GISQCWorkbenchCLI.exe`
- `build-windows-release/Release/GISQCWorkbenchWinPreview.exe`

### CTest

结果：通过

- 12/12 passed
- 0 failed

覆盖：

- DatasetScanner
- RuleTemplateLoader
- TaskModel
- DatasetMetadataReader
- DatasetScanService
- ResultStatistics
- IssueCsvExporter
- ReportExporter
- RuleCheckEngine
- TaskSession
- TaskHistoryStore
- ExecutionPageIntegration

## 3. CLI 功能测试

### 3.1 正常基础包

输入：`qa_synthetic_data/01_正常基础包`

在显式设置 `GDAL_DATA/PROJ_DATA/PROJ_LIB` 环境变量后：

- Data sources：1
- Rules：70
- Issues：0
- Pass rate：100.0%
- 结果：通过

未显式设置环境变量时：

- Issues：1
- 问题：`C010201 坐标系统不符合要求，实际为{未发现坐标系}`
- 结论：存在运行时 GDAL/PROJ 环境配置隐患，见“遗留问题”。

### 3.2 缺失/命名/配套文件问题包

输入：`qa_synthetic_data/02_缺失_命名_配套文件问题包`

结果：通过，程序无崩溃，能够正确输出问题 CSV。

- Data sources：3
- Rules：70
- Issues：20
- Pass rate：71.4%

命中的典型问题：

- `A010103` 缺少 `文档资料`
- `A010103` 缺少 `元数据`
- `A010104` 缺少 `metadata.xml`
- `A010301` 空目录
- `A010302` `bad name.shp` 命名不规范
- `A010302` `中文 文件.shp` 命名不规范
- `A010303` Shapefile 缺少 `.shx/.dbf/.prj` 等配套文件
- `B010202` 缺失必选数据表
- `C010201` 坐标系缺失/不符合

CSV 输出：

- `qa_test_outputs_after_deploy/case2.csv`
- UTF-8 BOM：有
- 中文内容：正常

### 3.3 GPKG 空间几何问题包

输入：`qa_synthetic_data/03_GPKG空间几何问题包`

在显式设置 `GDAL_DATA/PROJ_DATA/PROJ_LIB` 环境变量后：

- Data sources：1
- Rules：70
- Issues：12
- Pass rate：82.9%
- 结果：通过

命中的典型问题：

- `B010202` 缺失默认模板要求的必选表
- `C020201` 碎线
- `C020302` 尖锐角
- `C020501` Z 值
- `C030201` 线重叠
- `C030202` 线相交/重叠

未显式设置环境变量时多出：

- `C010201` 坐标系统不符合要求，实际为 `{未发现坐标系}`

## 4. 导出测试

已生成导出文件：

- `qa_test_outputs_ascii/case1.csv`
- `qa_test_outputs_ascii/case2.csv`
- `qa_test_outputs_ascii/case3.csv`
- `qa_test_outputs_ascii/reports/qa_report.csv`
- `qa_test_outputs_ascii/reports/qa_report.html`
- `qa_test_outputs_ascii/reports/qa_report.xls`
- `qa_test_outputs_ascii/reports/qa_report.doc`

验证结果：

- CSV 含 UTF-8 BOM
- HTML/XLS/DOC 均含 UTF-8 BOM
- HTML 含 `<meta charset="utf-8">`
- 中文标题和问题描述可正常写入
- 导出文件非空

说明：本轮验证为文件级自动化检查，尚未在 Office/WPS 中人工打开确认排版。

## 5. 异常与边界测试

### 不存在目录

输入：`qa_synthetic_data/不存在目录`

当前结果：

- CLI 返回码：0
- Data sources：0
- Rules：0
- Issues：0
- 生成空 CSV

测试结论：不崩溃，但行为不符合测试预期。

建议：不存在目录应返回非 0，并提示“目录不存在/不可读取”。

## 6. 发现的问题

### BUG-001：CLI 输出路径为中文时实际没有生成 CSV

现象：

- CLI 控制台显示 CSV 路径已输出
- 但当输出文件名包含中文，例如 `01_正常基础包.csv` 时，实际文件不存在
- 改为 ASCII 文件名 `case1.csv` 后可以正常生成

影响：中高

风险：客户选择中文导出路径或中文文件名时，可能误以为导出成功。

建议：CLI/导出文件写入需使用 Windows 宽字符路径或统一 pathFromText 转换，不要直接 `std::ofstream(std::string)` 写中文路径。

### BUG-002：不存在目录返回成功且规则数为 0

现象：

- 输入不存在目录时 CLI 返回 0
- 没有错误提示
- 输出空 CSV

影响：中

建议：执行前检查输入路径是否存在、是否为目录；失败时返回 1 并输出错误信息。

### BUG-003：未显式设置 PROJ/GDAL 环境变量时 GPKG 坐标系可能识别失败

现象：

- 交付目录已包含 `proj/proj.db` 和 `gdal/share/gdal/tms_NZTM2000.json`
- 但直接运行生成/CLI 时仍可出现：
  - `Cannot find tms_NZTM2000.json (GDAL_DATA is not defined)`
  - `Cannot find proj.db`
- 由此导致正常 GPKG 被报 `C010201 坐标系统不符合要求，实际为{未发现坐标系}`
- 显式设置 `GDAL_DATA/PROJ_DATA/PROJ_LIB` 后，正常基础包 Issues 从 1 变为 0

影响：高

建议：程序启动最早阶段统一设置 GDAL/PROJ runtime 路径，或在启动脚本/打包配置中设置环境变量。当前仅部分核心代码内配置，可能晚于 GDAL/PROJ 初始化或未覆盖辅助工具。

### ISSUE-004：报告导出文件级检查通过，但 Office/WPS 排版未人工验证

影响：低/中

建议：交付前用 WPS/Office 手动打开 `.xls/.doc/.html/.csv` 各一次，确认无乱码、无安全拦截、排版可接受。

## 7. 总体结论

当前版本核心引擎、规则执行、CSV/报告导出、中文数据路径扫描整体可用，自动化测试 12/12 通过。

但从测试工程师视角，当前还不建议直接声明“完全验收通过”。建议先修复以下 P0/P1 问题后再重新打包：

1. P0：CLI/导出中文输出路径不生成文件的问题。
2. P1：不存在目录返回成功的问题。
3. P1：GDAL/PROJ 运行时环境配置需要更稳定，避免正常 GPKG 坐标系误报。

修完后建议重新执行：

- Release build
- CTest 12/12
- 三组合成数据 CLI 测试
- 中文输出路径 CSV 测试
- 不存在目录错误返回测试
- Office/WPS 人工打开导出报告
